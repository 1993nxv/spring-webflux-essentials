version: '3.1'

services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: webfluxapi
      MYSQL_USER: delmondes
      MYSQL_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - webflux_data:/var/lib/mysql
volumes:
  webflux_data: